export interface IVersionRes {
    version: string
    nodeCount: number
    stakeAmount: number
}
export interface IVersion {
    version: string
    nodeCount: number
    stakeAmount: number
    stakePercent?: number
}
